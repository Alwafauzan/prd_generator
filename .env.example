# Penyimpanan = file-based di folder result/ (tanpa database).

# Optional: hanya jika pakai engine Anthropic API (bukan Claude Code CLI).
# ANTHROPIC_API_KEY=sk-ant-...
# Optional engine override: claude-code | api | preview
# PRD_ENGINE=claude-code
